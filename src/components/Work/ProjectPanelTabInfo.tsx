import type React from "react";

interface ProjectPanelTabInfoProps {
    name?: string;
}

const ProjectPanelTabInfo: React.FC<ProjectPanelTabInfoProps> = ({ name }) => {

    if (name != "") {

    } else {

    }
    return (
        <>
            klk mundo
        </>
    );
}

export default ProjectPanelTabInfo;